# W3 主链验收记录

记录日期：2026-03-13  
版本边界：Week3 / Phase 1（主链跑通）  
执行范围：代码 + 文档

## 1. 验收项与结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| `Triage Planner` 输入/输出协议已冻结 | 通过 | `docs/W3_主链跑通交付.md` 第2节 |
| 4 路径到 Tool 顺序唯一映射 | 通过 | `docs/W3_主链跑通交付.md` 第3节 |
| `/api/agent/*` 5 个接口已落地 | 通过 | `backend/app.py` 的 `api_create_agent_run/api_get_agent_run/api_get_agent_events/api_get_agent_result/api_retry_agent_run` |
| 失败节点重试语义已落地 | 通过 | `backend/app.py` 的 `_queue_agent_retry` 与 `AGENT_TOOL_RETRY_LIMITS` |
| 模态别名归一化含 `dcat` | 通过 | `backend/app.py` 的 `_normalize_uploaded_modalities` |
| 路径判定优先级固定 | 通过 | `backend/app.py` 的 `_build_path_decision` |
| 上传接口兼容不破坏 | 通过 | `backend/app.py` 的 `api_upload_start` 保持原返回并新增 `agent_run_id` |

## 2. 测试执行记录

### 2.1 静态检查
1. `python -m py_compile backend/app.py`：通过。

### 2.2 运行验证
1. 受当前环境缺少 `torch` 影响，未执行端到端运行测试（导入 `backend.app` 时依赖缺失）。
2. 本轮采用“静态校验 + 路由/函数链路检查”完成 Week3 验收基线。

## 3. 结论
1. Week3 代码与文档交付项已完成并冻结。
2. 当前可作为 Week4（ICV）输入。
3. 建议在具备完整模型依赖环境后补跑端到端回归：
   - 4 路径主链执行
   - 失败节点重试
   - `/api/agent/*` 接口联调

---

## 4. Week3补全验收（可见化，2026-03-13）

### 4.1 已完成项（代码实现）
- [x] 上传页增加 Agent 主链开关（默认开启），并透传 `start_agent_run=true`
- [x] processing page switched to Agent summary card view (single timeline + Agent summary)
- [x] processing 页接入 `/api/agent/runs/{run_id}` 与 `/events` 轮询
- [x] processing 页接入失败步骤重试入口（`retryable=true` 才可重试）
- [x] 后端增加统一 `[AGENT]` 结构化日志

### 4.2 本地检查记录
1. `python -m py_compile backend/app.py`：通过。
2. `node --check static/js/upload.js`：通过。
3. `node --check static/js/processing.js`：通过。

### 4.3 API烟测记录（最小）
1. `GET /api/agent/runs` 返回 `405`（路由存在且方法受限）。
2. `POST /api/agent/runs` 非法参数返回 `400`（输入校验生效）。
3. 历史样例 run：`e2c8aedd-8078-4aa9-8ffb-e4b0ba13a677`，可查询状态与事件。

### 4.4 待你手工确认（前端可见性）
1. 上传后页面显示 `agent_run_id`。
2. processing 页可看到 Agent 状态实时变化。
3. Left step-6 is mapped by Agent status (running/completed/failed), no longer stuck on `skipped`.
4. `Retry failed step` is shown only when `run.status=failed` and a `retryable=true` failed step exists; `attempt` increases after retry.
5. Agent toggle OFF keeps legacy upload-chain behavior.

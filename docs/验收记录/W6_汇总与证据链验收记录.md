# W6 汇总与证据链验收记录

更新时间：2026-03-23  
记录状态：Template created (pending runtime evidence)

## 1. 验收用例记录模板
请按用例逐条填写以下字段：
- `run_id`
- `job_id`
- `file_id`
- `imaging_path`
- 请求摘要（关键接口 + 关键响应字段）
- 终端日志片段（`[AGENT] / [SUMMARY] / [EVIDENCE]`）
- 截图路径（Processing / Validation / Report）
- 结论（PASS/FAIL）

---

## 2. 用例 A：NCCT-only（CTP claim unavailable）
- run_id：
- job_id：
- file_id：
- 请求摘要：
- 关键响应：
  - `report_payload.final_report`：
  - `report_payload.traceability`：
  - `report_payload.evidence_map`：
- 日志片段：
- 截图路径：
- 结论：

## 3. 用例 B：NCCT+mCTA（6 类结论映射）
- run_id：
- job_id：
- file_id：
- 请求摘要：
- 关键响应：
  - `coverage`：
  - `mapped_findings/total_findings`：
  - `high_risk_unmapped_count`：
- 日志片段：
- 截图路径：
- 结论：

## 4. 用例 C：NCCT+mCTA+CTP（量化对齐 + citation）
- run_id：
- job_id：
- file_id：
- 请求摘要：
- 关键响应：
  - core/penumbra/mismatch 对应 citation（doc/page）：
- 日志片段：
- 截图路径：
- 结论：

## 5. 用例 D：EKV/Consensus 强制失败（非阻断）
- run_id：
- job_id：
- file_id：
- 触发方式（如环境变量）：
- 关键响应：
  - run status：
  - `final_report.uncertainties`：
- 日志片段：
- 截图路径：
- 结论：

---

## 6. 阈值核对
- [ ] `traceability.coverage >= 0.90`
- [ ] 高风险结论 100% 可追溯或有 unavailable reason
- [ ] Validation 与 Report 的 traceability 展示一致
- [ ] `events` 可见 `icv -> ekv -> consensus/skip -> summary -> done`

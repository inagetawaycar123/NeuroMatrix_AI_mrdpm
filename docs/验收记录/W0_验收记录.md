# W0 验收记录（范围冻结与契约对齐）

## 1. 基本信息
- 周期：2026-04-13 ~ 2026-04-15
- 目标：规范冻结 + 骨架可跑

## 2. 验收用例
- [x] 用例 1：可从任务入口创建 run，并看到 Orchestration 计划面板
- [x] 用例 2：Runtime 区可显示 run 状态、stage、current_tool 占位信息
- [x] 用例 3：事件流按 `event_seq` 排序展示，状态标签一致
- [x] 用例 4：后端字段缺失时，前端回退渲染不白屏
- [x] 用例 5：输出完整 W1 backlog（P0/P1/P2）

## 3. 关键证据
- 路由：`/strokeclaw/w0`（任务入口）
- API：
  - `GET /api/agent/runs/{run_id}` 增量字段已输出
  - `GET /api/agent/runs/{run_id}/events` 增量字段已输出
  - `POST /api/agent/runs/{run_id}/review` 协议占位已返回 501
- 文档：
  - `W0范围冻结清单.md`
  - `W0页面IA与文案清单.md`
  - `W0状态机与事件字典.md`
  - `W0_API契约表_增量.md`
  - `W1_任务分解_P0_P1_P2.md`

## 4. 已知风险（进入 W1）
- `review` 动作尚未落业务状态机（W1 实装）。
- Runtime 事件仍以轮询为主（SSE/WebSocket 非 W0 范围）。
- 部分事件语义基于映射推导，需在 W1 做更细粒度节点事件。

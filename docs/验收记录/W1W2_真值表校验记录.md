# W1/W2 真值表与触发矩阵校验记录

记录日期：2026-03-13  
记录范围：Week1（基线）+ Week2（Tool 契约）  
记录类型：文档验收记录（非代码测试）

## 1. 校验输入来源
1. `docs/W1_基线加固交付.md`
2. `docs/W2_Tool契约交付.md`
3. `docs/Agent_PLAN.md`
4. `docs/卒中先锋_agent落地路线图_执行版.md`
5. `docs/每周任务清单_一页版.md`

## 2. Week1 校验结果
### 2.1 4 路径定义唯一性
- 结论：通过
- 核验点：
  - `NCCT-only`
  - `NCCT + single-phase-CTA`（三子场景）
  - `NCCT + mCTA`
  - `NCCT + mCTA + CTP`

### 2.2 触发规则正确性
- 结论：通过
- 核验点：
  - 仅 `NCCT+mCTA`、`NCCT+mCTA+CTP` 触发 `run_stroke_analysis`
  - 仅 `NCCT+mCTA` 触发 `generate_ctp_maps`
  - `NCCT+mCTA+CTP` 直接使用真实 CTP

### 2.3 字段语义冻结
- 结论：通过
- 核验点：
  - 模态判定主字段：`available_modalities`
  - 偏侧判定主字段：`hemisphere`
  - `hemisphere` 枚举：`right|left|both`
  - 原始值/归一化值双轨规则已写入

## 3. Week2 校验结果
### 3.1 7 个 Tool 字段完整性
- 结论：通过
- 核验点：每个 Tool 均包含 `applicability` 与 `precondition`

### 3.2 4 路径触发矩阵唯一顺序
- 结论：通过
- 核验点：
  - 判定优先级：`ncct_mcta_ctp -> ncct_mcta -> ncct_single_phase_cta -> ncct_only`
  - 每条路径的 Tool 顺序无二义性

### 3.3 错误码字典
- 结论：通过
- 核验点：每个错误码均定义 `retryable` 与 `suggested_action`

## 4. 文档一致性校验
- 结论：通过
- 核验点：
  1. `NCCT+mCTA`、`NCCT+mCTA+CTP` 的触发描述在 5 份文档一致
  2. `hemisphere` 枚举仅出现 `right|left|both`
  3. `/api/agent/*` 保持“本轮不实现、Week3后实现”的一致声明

## 5. 结果签署
- Week1：通过（可视为重做完成）
- Week2：通过（可视为重做完成）
- 可进入 Week3：是（以重做后的 W1/W2 文档为唯一输入）
